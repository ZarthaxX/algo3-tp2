#include <iostream>
#include <vector>

#include "expansivo.h"

using namespace std;

namespace Expansivo{

    Coloring expansivo(Graph& G, Graph& H){

        return Coloring(G.getNodeCount(),-1);
    }

}